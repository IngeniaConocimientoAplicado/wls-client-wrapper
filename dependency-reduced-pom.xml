<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/maven-v4_0_0.xsd">
  <modelVersion>4.0.0</modelVersion>
  <groupId>com.ingenia.utils</groupId>
  <artifactId>wlclient-wrapper</artifactId>
  <packaging>bundle</packaging>
  <name>Weblogin Client Wrapper</name>
  <version>1.0.0-SNAPSHOT</version>
  <build>
    <resources>
      <resource>
        <directory>src/main/resources</directory>
      </resource>
    </resources>
    <plugins>
      <plugin>
        <artifactId>maven-resources-plugin</artifactId>
        <version>${maven.resource.plugin.version}</version>
      </plugin>
      <plugin>
        <artifactId>maven-shade-plugin</artifactId>
        <version>${maven.shade.plugin.version}</version>
        <executions>
          <execution>
            <phase>package</phase>
            <goals>
              <goal>shade</goal>
            </goals>
            <configuration>
              <artifactSet>
                <includes>
                  <include>*:*</include>
                </includes>
              </artifactSet>
            </configuration>
          </execution>
        </executions>
      </plugin>
      <plugin>
        <groupId>org.apache.felix</groupId>
        <artifactId>maven-bundle-plugin</artifactId>
        <version>${maven.budnle.plugin.version}</version>
        <extensions>true</extensions>
        <configuration>
          <instructions>
            <Embed-Dependency>*;scope=compile|runtime</Embed-Dependency>
            <Import-Package>javax.ejb;resolution:=optional,
                            javax.ejb.spi;resolution:=optional,
                            javax.jms,javax.transaction,
                            javax.transaction.xa,
                            javax.xml.stream,
                            *;resolution:=optional</Import-Package>
            <Export-Package>!javax.ejb,
                            !javax.ejb.spi,
                            !javax.jms,
                            !javax.transaction,
                            !javax.transaction.xa,
                            !javax.xml.stream,*</Export-Package>
          </instructions>
        </configuration>
      </plugin>
    </plugins>
  </build>
  <properties>
    <maven.shade.plugin.version>2.4.3</maven.shade.plugin.version>
    <wls.client.version>10.3.6.0</wls.client.version>
    <maven.budnle.plugin.version>3.2.0</maven.budnle.plugin.version>
    <maven.resource.plugin.version>2.7</maven.resource.plugin.version>
    <maven.compiler.source>1.7</maven.compiler.source>
    <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
    <maven.compiler.target>1.7</maven.compiler.target>
  </properties>
</project>

